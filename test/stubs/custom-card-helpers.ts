// Stub for custom-card-helpers — not installed in this project.
// Vite resolves the dynamic import("custom-card-helpers") string at
// transform time and errors if the package is absent; this stub satisfies
// the resolver so ha-components.ts can be imported in tests.
export const loadCardHelpers = undefined;
