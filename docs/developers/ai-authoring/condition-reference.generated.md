<!-- AUTO-GENERATED by bin/gen_ai_docs.py — do not edit by hand. Run: make ai-docs -->

# Condition reference

Every built-in condition usable under a scene's `when`. Each `when` key is one of these names; its value is that condition's predicate (or `null` to match anything). See `conditions-cookbook.md` for plain-English → predicate examples.

## `unavailable`

- **Purpose:** Matches when any of the listed entities is unavailable, unknown, or missing.
- **Input type:** `unavailable_predicate`
- **Priority:** 980 (higher conditions are evaluated first)
- **Help:** {entities: [entity_id, ...]} — matches when ANY listed entity is unavailable, unknown, or absent. None = match-anything.

## `script`

- **Purpose:** Matches by calling a HA script that returns {match: bool}.
- **Input type:** `script_predicate`
- **Priority:** 975 (higher conditions are evaluated first)
- **Help:** Object {script: 'script.<name>', args?: {...}, triggers?: [entity_id]}. The script must end with `stop:` + `response_variable:` pointing at a dict {match: bool}. True = match. None = wildcard. `triggers` lists entities to re-evaluate on, since the script itself is opaque.

## `template`

- **Purpose:** Matches by rendering a Jinja2 template against HA state to a boolean.
- **Input type:** `template_predicate`
- **Priority:** 970 (higher conditions are evaluated first)
- **Help:** Object {template: '{{ ... }}'}. The Jinja template is rendered against current state; the result is coerced to a boolean the same way Home Assistant does (true/yes/on/1/enable and nonzero numbers => match; everything else, including unknown/none/empty, => no match). None = wildcard.

## `state`

- **Purpose:** Matches a boolean expression over entity states with optional duration.
- **Input type:** `state_predicate`
- **Priority:** 950 (higher conditions are evaluated first)
- **Help:** Expression tree: atoms {kind: 'is'|'is_not'|'>'|'>='|'<'|'<=', entity_id, attribute?, states, for?}, groups {kind: 'and'|'or', items: [...]}, negation {kind: 'not', item}. For numeric ops, `states` carries a single numeric threshold as a string. None = match-anything.

## `people`

- **Purpose:** Matches who is (not) at home or in a named zone.
- **Input type:** `people_predicate`
- **Priority:** 925 (higher conditions are evaluated first)
- **Help:** {who: [person.*] (omit to match all persons), quant: 'any'|'everyone'|'nobody', where: 'home'|'zone.*', negate?: bool, for?: {h,m,s}}. None = match-anything.

## `occupancy`

- **Purpose:** Matches whether presence/occupancy sensors are active.
- **Input type:** `occupancy_predicate`
- **Priority:** 915 (higher conditions are evaluated first)
- **Help:** {sensors: [binary_sensor.*] (empty = match-anything), occupied: bool (default true), quant: 'any'|'all', for?: {h,m,s}, negate: bool (default false, inverts the whole match)}. None = match-anything.

## `day`

- **Purpose:** Matches based on the current date, weekday, or workday status.
- **Input type:** `day_predicate`
- **Priority:** 900 (higher conditions are evaluated first)
- **Help:** Object {include: [...], exclude: [...]}. Items: {kind: 'weekday', days}, {kind: 'day_of_month', days}, {kind: 'date', month, day}, {kind: 'date_range', from, to}, {kind: 'last_day'}, {kind: 'workday'}, {kind: 'holiday'}, {kind: 'first_workday'}, {kind: 'last_workday'}.

## `time_of_day`

- **Purpose:** Matches based on the current time of day relative to sun events.
- **Input type:** `time_of_day`
- **Priority:** 800 (higher conditions are evaluated first)
- **Help:** Structured JSON predicate: {period: id} | {from, to} | [..., ...]. Endpoints are {kind: 'time', hh, mm} or {kind: 'sun', anchor, offset_min}.

## `lux`

- **Purpose:** Matches whether ambient light sensors fall in a lux range.
- **Input type:** `lux`
- **Priority:** 775 (higher conditions are evaluated first)
- **Help:** {sensors: [sensor.*] (empty = match-anything), range: str (a named lux range) | min?: int, max?: int (inline band [min,max)), quant: 'any'|'all', negate: bool (default false, inverts the whole match)}. None = match-anything.

## `sun`

- **Purpose:** Matches the sun's angular position — elevation and compass azimuth.
- **Input type:** `sun_predicate`
- **Priority:** 750 (higher conditions are evaluated first)
- **Help:** Object {elevation: {min, max}, azimuth: {sectors: [...], ranges: [{from, to}]}}. Elevation in degrees [-90, 90]; either bound optional. Azimuth matches any selected 8-point compass sector (N, NE, E, SE, S, SW, W, NW) or custom circular range (from > to wraps past 360). At least one of elevation/azimuth is required.

## `weather`

- **Purpose:** Matches the current weather condition and attribute thresholds.
- **Input type:** `weather_predicate`
- **Priority:** 700 (higher conditions are evaluated first)
- **Help:** Object {groups: [group_id, ...], thresholds: [{attribute, op, value}]}. Matches when the current weather condition belongs to one of the selected `groups` (empty = any) and every threshold holds. Operators: < <= > >=.

<!-- END GENERATED -->
